import 'package:flutter/material.dart';

const gy = Colors.grey;
const w = Colors.white;
const R = Colors.red;
const b = Colors.black87;
const hyperlinkColor = Color(0xFF00376B);
const secondaryColor = Color.fromARGB(217, 233, 229, 229);
const secondaryDarkColor = Color(0xFF8E8E8E);

final buttonDecoration = BoxDecoration(
    color: w,
    border: Border.all(color: secondaryColor, width: 0.8),
    borderRadius: BorderRadius.circular(4));

// class padding_text extends StatelessWidget {
//   const padding_text({super.key});
//   final Text;
//   @override
//   Widget build(BuildContext context) {
//     return const Padding(
//       padding: EdgeInsets.symmetric(horizontal: 20),
//       child: Text('Public figure'),
//     );
//   }
//}
